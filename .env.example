# Required for Databricks CLI (bundles)
DATABRICKS_HOST="https://<your-workspace-url>"
DATABRICKS_TOKEN="<your-pat-token>"

# Existing cluster for spark_python_task (Compute → cluster → ID in URL or UI).
# Value must be a cluster id, not a SQL warehouse id.
BUNDLE_VAR_cluster_id="<cluster-id>"

# GitHub Actions: you can keep an existing secret named BUNDLE_VAR_WAREHOUSE_ID if its value is the cluster id.
